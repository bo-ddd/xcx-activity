{
    "permissions": {
        "openapi": []
    },
    "triggers": [
        {
            "name": "updateActivityStatus",
            "type": "timer",
            "config": "*/5 * * * * * *"
        }
    ]
}